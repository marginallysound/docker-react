sudo: required

services:
    - docker

before_install:
    - docker build -t matthewrivera/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true matthewrivera/docker-react npm run test 